{
  "name": "gjuplans-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.33.1",
    "@typescript-eslint/parser": "^8.33.1",
    "eslint": "^9.28.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "build:cms": "npm run build --workspace=cms",
    "build:content": "npm run build --workspace=content",
    "build:shared-types": "npm run build --workspace=@gjuplans/shared-types",
    "build:api-client": "npm run build --workspace=@gjuplans/api-client",
    "dev:cms": "npm run dev --workspace=cms",
    "dev:content": "npm run dev --workspace=content",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx,.astro",
    "format": "prettier --write . --ignore-path .prettierignore",
    "test": "npm run test --workspaces"
  }
}
